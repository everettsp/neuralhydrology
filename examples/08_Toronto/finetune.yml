# --- Experiment configurations --------------------------------------------------------------------

# experiment name, used as folder name
experiment_name: mts_finetuned

# files to specify training, validation and test basins (relative to code root or absolute path)
train_basin_file: camus_basins.txt
validation_basin_file: camus_basins.txt
test_basin_file: camus_basins.txt

# --- Training configuration -----------------------------------------------------------------------

# specify learning rates to use starting at specific epochs (0 is the initial learning rate)
learning_rate:
    0: 5e-4
    2: 5e-5	

# Number of training epochs
epochs: 32

finetune_modules:
- head

base_run_dir: c:\Users\everett\Documents\GitHub\neuralhydrology\examples\08_Toronto\runs\test_mts_camels_augmented_64_2606_092458